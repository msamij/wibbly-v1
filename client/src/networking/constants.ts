enum Urls {
  baseUrl = 'http://127.0.0.1:8000/',
  baseApiUrl = 'api/v1/',
  authUrl = 'api/v1/auth/',
  signupUrl = 'signup',
  loginUrl = 'login',
  logoutUrl = 'logout',
  isLoggedIn = 'isloggedin',
}

export default Urls;
